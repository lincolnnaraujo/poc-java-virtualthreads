# Config A (baseline) - platform threads.
# Uso: docker compose --env-file .env.platform up -d
SPRING_THREADS_VIRTUAL_ENABLED=false
BENCHMARK_MODE=platform
BENCHMARK_CONSUMER_MODE=platform
BENCHMARK_CONSUMER_CONCURRENCY=50
