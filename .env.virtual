# Config B - virtual threads.
# Uso: docker compose --env-file .env.virtual up -d
# UNICA diferenca vs .env.platform: os 4 toggles abaixo. Todo o resto e identico (P3).
SPRING_THREADS_VIRTUAL_ENABLED=true
BENCHMARK_MODE=virtual
BENCHMARK_CONSUMER_MODE=virtual
BENCHMARK_CONSUMER_CONCURRENCY=400
